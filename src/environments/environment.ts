// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBWOon1OCXyY1dTQu3ciz-TbHcHJ73ZOfA",
    authDomain: "bptracker-app.firebaseapp.com",
    databaseURL: "https://bptracker-app.firebaseio.com",
    projectId: "bptracker-app",
    storageBucket: "bptracker-app.appspot.com",
    messagingSenderId: "226403224527",
    appId: "1:226403224527:web:7ff358d14503f062"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
